var api = require("foxie-api"),
		mongoose = require("foxie-mongoose")

var schema = new mongoose.Schema({
	machineName:String,
	mount:String,
	theme:String
})

module.exports = mongoose.model("mount-theme",schema)
