var path = require("path")

exports = module.exports = function(metaPath){
	var rootModule = path.resolve(__dirname,"../")

	//inject the scripts exec path
	var obj = require(path.join(rootModule,metaPath));
	obj["execPath"] = path.resolve(path.join(rootModule,metaPath),"../index.js")

	return obj;
}
