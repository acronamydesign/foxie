//dynamicly create a private array and extend, use new instance("arrayname") and then log local.arrayname

//Usage example

//Step one: give a name and data access name
//
//var adminMenuModel = new saphireApi.local("make")

//Step two:
//
//	adminMenuModel.extend({
//		machineName: machineName,
//		friendlyName: title,
//		enabled: menuEnabled,
//		index: menuIndex
//	});

//Step Three: Access new data
//
//adminMenuModel.make

var localExtend = function (objectNameSpace){
	this[objectNameSpace] = []
	this.obj = {}
	this.extend = function(key,val){
		if(typeof key == "string"){
			this.obj[key] = val;
			this[objectNameSpace].push(this.obj)
		}
		else if(typeof key == "object"){
			this[objectNameSpace].push(key)
		}
	}
}

module.exports = localExtend
