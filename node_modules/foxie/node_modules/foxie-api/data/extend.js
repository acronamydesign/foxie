var data = []

function extend(namespace,key,val){
	if(!namespace&&!key&&!val) return this;
	if(this[namespace]==undefined){
		this[namespace] = {};
		this[namespace][key] = val;
	}
	else this[namespace][key] = val
}

$ = extend.bind(data)

module.exports = $;
