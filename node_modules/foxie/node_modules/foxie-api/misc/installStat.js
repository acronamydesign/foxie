var data = require("../data/extend.js"),
		installStat = data.install_stat

module.exports = function(cb){
	if(!cb){
		if(installStat) return true;
		else return false;
	}
	else{
		if(installStat) cb
	}
}
