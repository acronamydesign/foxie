var fs = require("fs");

module.exports = function(dirname,fileType){
	var arr = []
	fs.readdirSync(dirname).forEach(function(filename){
		if(~filename.indexOf(fileType)) arr.push(require(dirname+"/"+filename));
	});
	return arr;
}
