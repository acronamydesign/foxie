var api = require("foxie-api"),
		schema = require("js-schema")
		api.schema = api.data.schema

var page = schema({
	title:String,
	description:String,
	body:String,
	url:String
})

module.exports = page
