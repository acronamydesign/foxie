var api = require("foxie-api"),
		mongoose = require("foxie-mongoose")

var schema = new mongoose.Schema({
	theme:String,
	machineName:String,
	name:String,
	wrapper:String,
	classes:String,
	compiled:String,
	mixin:String
})

module.exports = mongoose.model("boxes-themes",schema)
